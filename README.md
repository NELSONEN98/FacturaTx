# FacturaTx

Reimplementación total de la web Facturatech con React + Next.js, reconstruida desde cero con enfoque en arquitectura moderna, performance y CSS sin frameworks (Vanilla CSS + Modules).

## Stack Tecnológico
- **Framework:** Next.js 15 (App Router)
- **Frontend:** React + TypeScript
- **Estilos:** CSS Modules + Variables CSS (Diseño Custom)
- **Fuentes:** Roboto (Google Fonts)

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

This project uses [`next/font`](https://nextjs.org/docs/app/building-your-application/optimizing/fonts) to automatically optimize and load Roboto.

## Estructura del Proyecto

- `/src/app`: Rutas y páginas (Login, Dashboard)
- `/src/components`: Componentes reutilizables (UI, Dashboard)
- `/src/app/globals.css`: Variables CSS y estilos globales
